<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= country ? country.name : 'Country Not Found' %></title>
    <style>
        body { font-family: Arial, sans-serif; padding: 40px; background: #f0f8ff; }
        .container { max-width: 800px; margin: 0 auto; }
        .error { background: #ffe6e6; padding: 20px; border-left: 5px solid #ff3333; }
        .country-header { display: flex; align-items: center; margin-bottom: 30px; }
        .flag { width: 100px; margin-right: 20px; border: 1px solid #ccc; }
        .info-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 20px; margin: 30px 0; }
        .info-card { background: white; padding: 20px; border-radius: 8px; box-shadow: 0 2px 5px rgba(0,0,0,0.1); }
        .back-btn { 
            display: inline-block; 
            padding: 10px 20px; 
            background: #3498db; 
            color: white; 
            text-decoration: none; 
            border-radius: 4px;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <% if (error) { %>
            <div class="error">
                <h2>⚠️ Error</h2>
                <p><%= error %></p>
                <a href="/" class="back-btn">← Back to Home</a>
            </div>
        <% } else { %>
            <!-- Country Header -->
            <div class="country-header">
                <img src="<%= country.flag %>" alt="Flag of <%= country.name %>" class="flag">
                <div>
                    <h1><%= country.name %></h1>
                    <p><em><%= country.officialName %></em></p>
                </div>
            </div>
            
            <!-- Country Information -->
            <div class="info-grid">
                <div class="info-card">
                    <h3>📍 Capital</h3>
                    <p><%= country.capital %></p>
                </div>
                <div class="info-card">
                    <h3>👥 Population</h3>
                    <p><%= country.population %></p>
                </div>
                <div class="info-card">
                    <h3>🗺️ Region</h3>
                    <p><%= country.region %></p>
                    <p><em><%= country.subregion %></em></p>
                </div>
                <div class="info-card">
                    <h3>💱 Currency</h3>
                    <p><%= country.currency %></p>
                    <p><em>Symbol: <%= country.currencySymbol %></em></p>
                </div>
                <div class="info-card">
                    <h3>🗣️ Languages</h3>
                    <p><%= country.languages %></p>
                </div>
                <div class="info-card">
                    <h3>⏰ Timezones</h3>
                    <p><%= country.timezones %></p>
                </div>
            </div>
            
            <!-- Actions -->
            <div style="margin-top: 40px;">
                <a href="/" class="back-btn">← Back to Home</a>
                <a href="/country?name=<%= country.name %>" class="back-btn" style="background: #2ecc71;">🔄 Reload</a>
            </div>
            
            <!-- Search Again -->
            <div style="margin-top: 40px;">
                <h3>Search Another Country</h3>
                <form action="/country" method="GET">
                    <input type="text" name="name" placeholder="Enter country name" style="padding: 10px; width: 300px;">
                    <button type="submit" style="padding: 10px 20px;">Search</button>
                </form>
            </div>
        <% } %>
    </div>
</body>
</html>